# Runtime dependencies. Versions are minimum-acceptable (>=) until the live env
# is finalized. Once a working environment is locked, replace with exact pins:
#     pip freeze > requirements.txt
torch>=2.0
torchaudio>=2.0
librosa>=0.10
audiomentations>=0.33
albumentations>=1.3
soundfile>=0.12
pydub>=0.25
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
seaborn>=0.13
matplotlib>=3.7
tqdm>=4.65
